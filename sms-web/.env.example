# URL of the FastAPI backend the browser calls.
# Leave unset in development to use the default http://127.0.0.1:8000.
# In production, set this to your deployed API URL at build time:
#   VITE_API_URL=https://your-api.example.com npm run build
# VITE_API_URL=http://127.0.0.1:8000
